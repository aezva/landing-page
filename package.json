{
  "name": "nnia",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend/client-panel && npm install && cd ../../nnia_chat_widget && npm install",
    "dev:backend": "cd backend && uvicorn main:app --reload",
    "dev:client": "cd frontend/client-panel && npm run dev",
    "dev:widget": "cd nnia_chat_widget && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\" \"npm run dev:widget\"",
    "build:client": "cd frontend/client-panel && npm run build",
    "build:widget": "cd nnia_chat_widget && npm run build",
    "build": "npm run build:client && npm run build:widget",
    "start": "cd backend && uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "cd backend && pytest && cd ../frontend/client-panel && npm test && cd ../../nnia_chat_widget && npm test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "clsx": "^2.1.1",
    "concurrently": "^8.2.2",
    "lucide-react": "^0.501.0",
    "next-intl": "^4.0.2",
    "tailwind-merge": "^3.2.0"
  }
}
